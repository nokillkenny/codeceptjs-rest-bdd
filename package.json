{
  "name": "codeceptjs-rest-bdd",
  "version": "1.0.0",
  "scripts": {
    "test": "codeceptjs run --features --steps",
    "test:report": "codeceptjs run --features --steps --reporter mochawesome"
  },
  "devDependencies": {
    "codeceptjs": "^3.6",
    "mochawesome": "^7.1"
  }
}
